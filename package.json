{
  "name": "next-server",
  "version": "1.0.0",
  "description": "A simple JSON Server hosted on Glitch",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "express": "^4.17.1",
    "json-server": "^0.17.0"
  },
  "engines": {
    "node": "14.x"
  }
}
